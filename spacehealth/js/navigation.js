// Navigate to login page with role pre-selected
function navigateToLogin(role) {
  window.location.href = `pages/login.html?role=${role}`;
}

// Navigate to register page with role pre-selected
function navigateToRegister(role) {
  window.location.href = `pages/register.html?role=${role}`;
}
